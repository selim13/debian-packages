{
  "name": "site",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "eleventy --serve dotenv_config_path=../.env",
    "build": "eleventy dotenv_config_path=../.env --output=../public"
  },
  "devDependencies": {
    "@11ty/eleventy": "^0.12.1",
    "prettier": "^2.2.1"
  },
  "dependencies": {
    "debian-control": "^1.0.0",
    "dotenv": "^8.2.0"
  }
}
